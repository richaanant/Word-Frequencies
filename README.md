# Word-Frequencies
Implement a TextDocument class to process text files, compute metrics like word count, sentence count, word frequencies, and syllable counts, and store results in a hash table. Useful for comparing document content and building search or language processing applications.
